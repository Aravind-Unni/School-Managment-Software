{
  "module_id": "M03",
  "slug": "timetable",
  "_comment": "M03 owns no asynchronous work, so it starts no broker and no worker: several standalone stacks must be able to run at once.",
  "_status": "implemented; backend, migrations and React feature routes are served by `dev.py up M03 --profile standalone`",
  "resources": {
    "postgres": true,
    "frontend": true,
    "broker": false,
    "worker": false,
    "object_storage": false
  },
  "seed_scenarios": [
    "baseline"
  ]
}
