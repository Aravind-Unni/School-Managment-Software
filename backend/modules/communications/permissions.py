"""Permission codes owned by M11. Must match ModuleRegistration and Access rules."""

PERMISSION_CODES: tuple[str, ...] = (
    "notices.create",
    "notices.publish",
    "messages.send",
    "messages.read_status",
    "sms.configure",
)
