"""Permission codes owned by M06. Must match ModuleRegistration and Access rules."""

PERMISSION_CODES: tuple[str, ...] = (
    "performance.read",
    "warnings.manage",
    "interventions.manage",
    "meetings.record",
    "observations.read_sensitive",
)
