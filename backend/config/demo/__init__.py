"""Demo data for trying the product in a browser. Synthetic, never real people.

Everything is created through the product's own HTTP API, acting as the real
accounts (owner, teachers, accountant, librarian), so the data obeys every
rule the product enforces. Refuses to run unless explicitly allowed.
"""
